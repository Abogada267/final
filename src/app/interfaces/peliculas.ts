export interface Pelicula {
    id?:string,
    titulo: string | null,
    genero: string | null,
    year: number | null,
    stock: number | null,
    precio: Number | null,
    foto: string | null,
}
